{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Chrome UI webview talks to the core (tab + navigation commands, tab events).",
  "windows": ["main"],
  "webviews": ["chrome"],
  "permissions": ["core:default", "deep-link:default"]
}
